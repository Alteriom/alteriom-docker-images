# Repository Metadata Manager - Environment Variables
# Copy this file to .env and fill in your actual values

# NPM Authentication Token (for publishing packages)
# Get from: https://www.npmjs.com/settings/tokens
# Required permissions: Automation tokens or Granular access tokens with publish scope
NPM_TOKEN=npm_your_token_here

# GitHub API Token (for repository metadata operations)  
# Get from: https://github.com/settings/tokens
# Required permissions: repo (full repository access) or public_repo for public repositories
GITHUB_TOKEN=ghp_your_token_here

# Repository identification (auto-detected from git if not set)
# These values should match your actual repository
GITHUB_REPOSITORY_OWNER=YourUsername
GITHUB_REPOSITORY_NAME=your-repo-name

# Organization settings
# Update these to match your organization/project branding
ORGANIZATION_TAG=your-org
ORGANIZATION_NAME=Your Organization Name

# Optional: Docker Registry Configuration
# Uncomment and configure if using custom registry
# DOCKER_REGISTRY=ghcr.io
# DOCKER_NAMESPACE=your-username/your-repo

# Optional: Build Configuration
# Uncomment and configure for custom build settings
# BUILD_CONTEXT=.
# DOCKERFILE_PATH_PROD=production/Dockerfile
# DOCKERFILE_PATH_DEV=development/Dockerfile
